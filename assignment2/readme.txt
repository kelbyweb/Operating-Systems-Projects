Kelby Webster 

For assignment #2:

To execute lab2.c:

gcc -o lab2 lab2.c

Then ./lab2

Then enter given input commands as desired. 

Given test cases:

$ ls -t -al

$ ls > output.txt

$ cat output.txt

Check if the content of output.txt is correct or not

$ ./pre <input.txt | ./sort > output.txt

./pre and ./sort are the executable from the first assignment. 

$ ls | more | wc

$ exit
